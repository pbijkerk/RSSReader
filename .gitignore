# Xcode build output
build/
DerivedData/
*.xcuserstate

# Gebruikersspecifieke Xcode-state (schema-volgorde, zichtbaarheid) — per machine anders
RSSReader.xcodeproj/xcuserdata/

# macOS
.DS_Store

# SourceKit-LSP / xcode-build-server (machine-specifiek)
buildServer.json
.sourcekit-lsp/
